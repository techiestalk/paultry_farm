"""
    Database Chicken feed models
"""


class Feed:
    """
        FEED table schema:  FEED_ID, FEED_NAME

        Create class methods for the below methods:

        1. def add_feed(cls, feed_name) - Should add the given feed to the Feed Table
        2. def update_feed(cls, feed_name, new_feed_name) - Rename the existing feed name to new name
        3. def delete_feed(cls, feed_name) - Delete given feed from table
        4. def get_all_feed(cls, feed_name) - Query the database and return the feed names as list

    """


class Medicine:
    pass
